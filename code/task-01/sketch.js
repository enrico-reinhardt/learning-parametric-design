// PRELOAD ASSETS
// LOAD ASSETS > WHEN LOADED EXECUTE SETUP
function preload() {}

// SETUP > ONCE
function setup() {
    createCanvas(400, 400)
}

// THEN > DRAW > LOOP
function draw() {
    background(255, 128, 0)
    rect(200, 0, 10, 400)
}
